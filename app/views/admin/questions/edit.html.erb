<%= simple_form_for([:admin, @question.examination, @question], html: {class: 'form-holizonal'}) do |f| %>
  <%= render 'form', f: f %>
  <%= button_tag :submit, class: 'btn btn-primary' do %>
    <i class="icon-ok icon-white"></i> 決定
  <% end %>
<% end %>
